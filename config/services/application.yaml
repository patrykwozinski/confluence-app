imports:
    - { resource: 'command_handlers.yaml' }
    - { resource: 'command_validators.yaml' }

services:
    command_locator:
        class: App\Application\CommandLocator

    command_bus:
        class: App\Application\CommandBus
        arguments: ['@command_locator']
